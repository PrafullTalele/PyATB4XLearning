print(max(10, 23, 45, -1, -2, 100, 1, 87.34, "Pramod"))
#There should be same type of data for comparison.
#(str, int) -> can't compare (will give error because data is not same).
#(int, int) -> can compare (this is the right way).
#(str, str) -> can compare (this is the right way).

