c = "C" # Even if it has only one character, python will think it is string only.
print(c) # Data type will be string (str)


age = 45
print(type(age))
age = "Pramod"
print(age)
print(type(age))
 #It will print Pramod as an output because Python is a Dynamically typed language.
# Dynamically Typed -> At a run time, data type can be changed.
# we don't need to mention the data type. -> Because Python interpreter is very smart that it will automatically
#change the data type.
